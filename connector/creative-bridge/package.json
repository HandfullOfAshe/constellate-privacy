{
  "name": "creative-bridge-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP connector for claude.ai: Envato Market API tools plus an opt-in Suno-compatible adapter, gated by OAuth 2.1 (PKCE + dynamic client registration).",
  "scripts": {
    "dev": "netlify dev",
    "local": "node test/local.mts",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "@netlify/blobs": "^10.0.0",
    "@netlify/functions": "^4.0.0",
    "zod": "^4.0.0"
  }
}
